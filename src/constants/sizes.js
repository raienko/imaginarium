import {rem} from 'src/utils/units';

export const offset = rem(10);

export const hitSlop = {
  top: offset,
  bottom: offset,
  left: offset,
  width: offset,
};

export const borderRadius = rem(15);

export const elementWidth = rem(320);

export const elementHeight = elementWidth * 0.23;

export const elementFontSize = rem(24);
